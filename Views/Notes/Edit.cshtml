@model OnlineSchoolWebApp.Models.Note

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Note</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="NoteId" />
            <div class="form-group">
                <label asp-for="NoteValue" class="control-label"></label>
                <input asp-for="NoteValue" class="form-control" />
                <span asp-validation-for="NoteValue" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="NoteDesc" class="control-label"></label>
                <input asp-for="NoteDesc" class="form-control" />
                <span asp-validation-for="NoteDesc" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EtudiantId" class="control-label"></label>
                <input asp-for="EtudiantId" class="form-control" />
                <span asp-validation-for="EtudiantId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CoursId" class="control-label"></label>
                <select asp-for="CoursId" class="form-control" asp-items="ViewBag.CoursId"></select>
                <span asp-validation-for="CoursId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
