@model IEnumerable<OnlineSchoolWebApp.Models.Departement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th>
               Année Scolaire
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Annee.DateDebut)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DepartementId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DepartementId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DepartementId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
